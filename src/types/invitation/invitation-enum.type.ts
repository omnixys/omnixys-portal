export enum InvitationStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  DECLINED = "DECLINED",
  CANCELED = "CANCELED",
  REJECTED = "REJECTED",
  APPROVED = "APPROVED",
  EXPIRED = "EXPIRED",
}

export enum RsvpChoice {
  YES = "YES",
  NO = "NO",
  MAYBE = "MAYBE",
}
